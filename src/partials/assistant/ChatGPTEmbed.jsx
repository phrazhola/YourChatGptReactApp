import React from 'react';

import ChatbotUI from '../../vendors/chatgpt_frontend/src/App';

const ChatGPTEmbed = () => {
  //const queryClient = new QueryClient();

  return (
    //<QueryClientProvider client={queryClient}>
    <ChatbotUI />
    //</QueryClientProvider>
  );
};

export default ChatGPTEmbed;
